{
    "name": "tulsi-inventory-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "postinstall": "cd frontend && npm install",
        "build": "cd frontend && npm run build"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}